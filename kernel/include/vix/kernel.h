#pragma once
/* this function must be called from the entry point when the system has set up the print functions, read the memory map and is ready to initialize the memory manager */
void kernelstart();
